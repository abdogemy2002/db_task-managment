{
  "name": "db",
  "version": "1.0.0",
  "description": "# db_task-managment",
  "main": "index.js",
  "scripts": {
    "start": "chmod +x node_modules/.bin/json-server && json-server --watch db.json --port ${PORT:-3000}"  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
